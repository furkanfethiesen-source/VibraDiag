parent_child_retrieval:
  chunking:
    child_chunk_size: 200
    child_chunk_overlap: 20
    parent_chunk_size: 1500

hybrid_search:
  reranker:
    model: "BAAI/bge-reranker-large"
    batch_size: 32
    top_k: 3
    score_threshold: 0.20
    soft_fallback_floor: 0.10
    visual_fallback_threshold: 0.20
    target_candidate_budget: 35
    min_candidates_per_subquery: 8

qdrant:
  persist_path: "./qdrant_data"
  text_collection: "vibra_text_child"
  visual_collection: "vibra_visual"
  model_serving: "local"  # "local" | "tei" (TEI ertelenmiştir)

sparse_encoder:
  model: "Qdrant/bm42-all-minilm-l6-v2-attentions"

docstore:
  persist_path: "./docstore.db"